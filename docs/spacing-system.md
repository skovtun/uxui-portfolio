# Spacing System

**Status:** Source of Truth  
**Purpose:** Единая система отступов и спесинга для всего сайта

---

## 🎯 ПРИНЦИПЫ

Система основана на модульной сетке **8px** для консистентности и визуального ритма.

---

## 📐 СТАНДАРТНЫЕ ОТСТУПЫ

### Заголовки

- **H2 (SectionHeader)**: `mb-10` (40px)
  - Отступ после заголовка до контента
  
- **H3 (SubsectionHeader)**: `mb-8` (32px)
  - Отступ после заголовка до контента

- **H3 (inline)**: `mb-8` (32px)
  - Для H3 заголовков, не использующих SubsectionHeader компонент

### Текст

- **Параграф после заголовка**: `mb-6` (24px)
- **Параграф между параграфами**: `mb-6` (24px)
- **Параграф перед изображением**: `mb-6` (24px) + `mt-10` (40px) на контейнере изображений

### Изображения

- **ImageWithCaption**: `mb-12` (48px)
  - Единый отступ после каждого изображения с подписью
  
- **Отступ от текста до изображений**: `mt-10` (40px)
  - Применяется на контейнере с изображениями

- **Между изображениями**: Автоматически через `mb-12` в ImageWithCaption

### Секции

- **Между основными секциями**: `space-y-12 md:space-y-16` (48-64px)
  - В основном контейнере статьи

---

## ✅ ПРАВИЛА ПРИМЕНЕНИЯ

1. **Всегда используй компоненты**:
   - `SectionHeader` для H2
   - `SubsectionHeader` для H3
   - `ImageWithCaption` для изображений с подписями

2. **Не добавляй дополнительные отступы**:
   - ImageWithCaption уже имеет `mb-12`
   - Не оборачивай ImageWithCaption в div с дополнительными отступами

3. **Консистентность**:
   - После любого заголовка до текста: стандартный отступ компонента
   - После текста до изображений: `mt-10` на контейнере
   - Между изображениями: автоматически через `mb-12`

---

## 🚫 ЧТО НЕ ДЕЛАТЬ

❌ Использовать `mt-8` или `mt-6` для контейнеров с изображениями  
❌ Использовать `space-y-6` для контейнеров с ImageWithCaption  
❌ Добавлять дополнительные обертки вокруг ImageWithCaption  
❌ Смешивать разные системы отступов в одном компоненте

---

**Этот документ — Source of Truth. От него не отходим.**

